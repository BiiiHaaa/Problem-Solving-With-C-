#include <iostream>
#include <cctype>
#include <cmath>
#include <algorithm>
#include <chrono>
#include <vector>
using namespace std;
int main () {
    /*vector<int> Number = {10 , 20  , 30};
    for(int i=0;i<Number.size();i++) {
        cout << " " << Number.at(i) << " ";
    }
    cout << endl<<Number.size()<<endl;
    Number.push_back(40);
    for(int i=0;i<Number.size();i++) {
        cout << " " << Number.at(i) << " ";
    }   
    cout << endl<<Number.size()<<endl;
    for(int i=0;i<Number.size();i++) {
        cout << " " << Number.at(i)*10 << " ";
    }
    */

   cout<<"===========================";
   
    /*
   void Vector(vector<int>Number) {
    int Result = 0;
    for(int i=0;i<Number.size();i++) {
        Result+=Number[i];
    }
    cout << "Result = "<<Result;
    }
   vector<int> VectorOfNumber = {100 , 200 , 300 , 400 , 500};
   Vector(VectorOfNumber);
   */
}